apiVersion: v1
kind: Service
metadata:
  name: whatsapp-api
  labels:
    app.kubernetes.io/name: whatsapp-api
    app.kubernetes.io/part-of: whatsapp-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: whatsapp-api
  ports:
    - name: http
      port: 80
      targetPort: http
